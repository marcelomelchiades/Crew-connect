import { PlaceholderPage } from "@/components/PlaceholderPage";

const SettingsPage = () => (
  <PlaceholderPage
    title="Settings"
    description="Manage users, roles, transport companies, and system configuration."
  />
);
export default SettingsPage;
